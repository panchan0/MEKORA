{
  "name": "mekora",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "description": "MEKORA web roguelike with a modular runtime and GitHub Pages deployment.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "verify": "node ./scripts/verify-project.mjs",
    "build:static": "node ./scripts/build-static-preview.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
